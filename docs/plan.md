# Plan / open work

- App Store Connect listing for "EvenBetterRYM for Safari" (iOS + macOS,
  v1.0) is still in "Prepare for Submission" — metadata (description, promo
  text, copyright, support URL, age ratings, content rights) has been filled
  in; see `CLAUDE.md`'s "App Store Connect metadata" section for the exact
  values chosen and why. Not yet submitted for review as of 2026-07-26.

(Safari iOS manifest `persistent` flag fix landed in commit `3d803f30` — see
`docs/codebase.md` for the reasoning if needed.)
