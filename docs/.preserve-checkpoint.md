# Preserve checkpoint

Last reviewed: 2026-08-06

Last topic covered: bringing `chart-searchbar`'s `chart-shortcuts`
work over to `main` — the `git cherry -v` triage that excluded 3
independently-diverged commits, cherry-picking the remaining 17 with
doc-conflict resolution (including a "wholesale duplicate block" case),
squashing into one commit (`f96852bd`) per the user's explicit ask, the
`biome.json` reference-dump/glob-escaping lint fixes it surfaced, the
accidental stash pop/restore of an unrelated pre-existing stash, and
pushing both `main` and `chart-searchbar` to `origin`.
