# Preserve checkpoint

Last reviewed: 2026-08-22

Last topic covered: a follow-up round on the `chart-shortcuts-customization`
branch — making chart-shortcut modifier key labels platform-native
(Option/Control/Command off Mac, native `⌃⌥⇧⌘` glyphs on Mac), the
`formatCombo`-takes-an-explicit-`mac`-parameter test-flakiness fix, the
monospace-vs-system-font glyph rendering fix and its still-unresolved
"squished" gap versus true native macOS rendering, and two rounds of
checkpoint commits (`bcd264e`/`35b556d` then `d69f073` in even-better-rym;
`ba4f145`, unrelated, in EvenBetterRYM/). This `/preserve` pass itself was
the last exchange reviewed.
