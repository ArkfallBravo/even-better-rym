# Preserve checkpoint

Last reviewed: 2026-08-23

Last topic covered: the artist-link-formatting feature session end-to-end —
reverse-engineering RYM's track-listing "Insert link to artist or work"
popup mechanism (`createShortcut`/`currentElement`, distinct from
`RYMbrowser`), designing and implementing the auto-format-on-insert
algorithm, the CLI-only Safari rebuild recipe (`xcodebuild` without Xcode
GUI), the "I dev for Safari mainly" user-memory save, the git-stash-based
commit sequencing correction (v1.2 pbxproj bump committed separately from
the session's feature work), and the `.gitignore` addition for `app store
submission stuff/v1.2/` and `resume stuff/` plus the CLAUDE.md staleness
fix that prompted. This `/preserve` pass itself was the last exchange
reviewed.
