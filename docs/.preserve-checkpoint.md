# Preserve checkpoint

Last reviewed: 2026-08-06

Last topic covered: `chart-prefix-commands` — debugging the "Ctrl+R/F/V don't
work" report (ruled out Mac-native-keybinding interception via a live console
probe, found it was input-focus scoping instead), the resulting redesign to a
single document-level keydown listener with `isOtherEditableTarget` guarding
against stealing keystrokes from other text fields, the final
`ADVANCED_USER_TOGGLE` key remap (F=following/V=followers/R=self), and the
commit (`50e985c7`) on `chart-searchbar`.
