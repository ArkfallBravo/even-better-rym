# Preserve checkpoint

Last reviewed: 2026-08-14

Last topic covered: the autofind-link triage session — finding the
`import-check` debug page, extending it with a `search-panel.tsx` for
the 8 `SEARCHABLES` finders, gating the whole debug page behind a
`VITE_DEBUG_TOOLS` build flag, fixing the hardcoded wrong
`getURL()` path that caused Safari's "Can't Find the File" error,
committing that work to `main` (`6b301cae`), and then committing the
private `EvenBetterRYM/` repo's accumulated state as a safety
checkpoint (`a8b383c`) before Xcode's "Update to recommended settings"
prompt.
