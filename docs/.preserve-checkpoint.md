# Preserve checkpoint

Last reviewed: 2026-09-06 (second pass, later same day)

Last topic covered: the Apple Music "Sorry :/" import bug on branch
`fix-apple-music-title-slash-mangling` — root-causing the doubled-slash in
`splitPhrases`, the back-and-forth on whether to strip " - EP" (ended: keep
stripping, it's Apple's format suffix), the emoticon-safe tokenizer fix
committed as `0942e7c`, moving the concurrent `/simplify` commit `afe3274`
onto `main` and fast-forwarding `main` to include the fix + pushing, and
creating the private GitHub repo `ArkfallBravo/even-better-rym-safari` as
the new remote for the `EvenBetterRYM/` Xcode repo. This `/preserve` pass
was the last exchange.
